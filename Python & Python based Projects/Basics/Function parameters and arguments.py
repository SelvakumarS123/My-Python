def greet(name): # here the 'name' is referred as parameter. The parameter is the name of the data that is being passed in.
    print(f"Hello, {name}")


name = str(input("Enter ur name:>\n "))
greet(name) # here 'name' is referred as the argument. Argument is the actual value of the data.
